dist: xenial
language: csharp
mono: none
dotnet: 2.2
script:
  - dotnet restore
  - dotnet add package Microsoft.Extensions.Configuration.Abstractions --version 2.2.0
  - msbuild /p:Configuration=Release location/location.sln
  - msbuild /p:Configuration=Release locationserver/locationserver.sln
